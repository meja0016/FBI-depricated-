/* 
package marx.maxplank.daimler.ibm;

import static org.junit.Assert.assertEquals;
import org.junit.Before;
import org.junit.Test;

public class TestJunit{

    private nonPoliceXML nonPoliceXML;

    @Before
    void setUp() throws Exception {
        nonPoliceXML nonPoliceXML = new nonPoliceXML();
    }

    @Test
    void testMultiply() {
        assertEquals( "Regular multiplication should work", nonPoliceXML.nonPoliceProcess(4,5), 20);
    }

    @Test
    void testMultiplyWithZero() {
        assertEquals("Multiple with zero should be zero",0,  nonPoliceXML.nonPoliceProcess(0,5));
        assertEquals("Multiple with zero should be zero", 0, nonPoliceXML.nonPoliceProcess(5,0));
    }
}
*/