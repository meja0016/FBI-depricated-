developer applciaotn database layer with excel( business intellicne(bi) sheet)

id,description
1,ibm.dmier.carl-benz
2,zelliendorf
3,pankau
4,ibm.damiler.paybytouch
5,ibm.damiel.haupstrasse(mainstret)
6,ibmdmiler.visa.(go - wall)
7,ibm.dmielr.appdyanmics
8,Quart(implementation)
9,Potamer Tor
10,Love parade Paots damer Tor
11,ibm.dmiler.dell.(philip gay from the bush( von dem busche)



Entity Bean.(findByPrimaryKey(id:1:ibm.damielr.carl-benz))
	     findByPrimaryText(carl-benz)
ibm.dmiler.eere(bottom-up,top-down,!meet in the middle)
@ejb.persistence


Java EE > EJB to RDB Mapping (1.x-2.x) > Generate Map.
https://www.ibm.com/docs/en/radfws/9.6.1?topic=tables-generating-top-down-mapping

sequia avg
  pojo,rake,php,mysql,npm?,railrubys,


public class ClientBusinessDelegete {

public static void main(String[] args) {
		ClientServiceLocator().getInstance().locate(""ejb/EmployeeHome"")
                EmployeeHome home = (EmployeeHome) PortableRemoteObject.narrow(obj, EmployeeHome.class);
            EmployeeBean bean = home.create();
            System.out.println("EJB do Output  busines delegation -> " + bean.Employee().createFile());}


2003(20Years) ibm.dmer.deere.pattners(servicelocator,
public class ClientServiceLocator {
public Service locate("ejb/EmployeeHome"){
	Hashtable env = new Hashtable();
env.put("org.omg.CORBA.ORBClass", "com.ibm.CORBA.iiop.ORB");
env.put("java.naming.factory.initial", "com.ibm.websphere.naming.WsnInitialContextFactory");
env.put("java.naming.provider.url", "corbaloc::www_websphere_1:2809");
Context ctx = new InitialContext(env);
Object obj = ctx.lookup("ejb/EmployeeHome");
}




jndi
 - schduler
 - ejb
 - persisnance
 - email
 - 


shell to z/OS
 nonPiceTmerlogia.maxplank.marx
   - create file, exec file, format output, 
   - war file, ear file, web serivce, 
			  - service catalog (aspx,)
	other: c++(z/TPF)
		assebley

ent.nonPoliceGreenSpanLogical.( logical combine attachment, rice )
     .nonPoliceDatabaseConnectionLogical.

	shell: graziani,rontodo,carolos,lukanvice,ward(est: 5-10 shell script/day): 1 reused / month
			resued/not resued
			mciheal king: not reused
		claudio:oralce(reused assuedm dba)
		mark tavis perfomrance: cannot run perl ro shell
		dave: use to be shell /perl manager, now IBM prsnee service orient arhcitere 2010
	java: not resued 
	j2ee: not rusued(intern jeorg)
	      assuemd reused( manfred)
	assmeble/z/os: ibm.dmielr.dere: used for payments, tractors facotry assmebl line
			not modifed: used(monitred), (pays by deefaut income since 1970)montoreded.

	Federal Govmente,Rice(john deere){
		(delta) (pays since 1970) rice: assembly
	}

